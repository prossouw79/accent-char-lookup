# Accent Character Lookup #

## Setup

```sh
npm install
```

## Run

```bash
# terminal 1 (UI localhost for hot-reload)
npm run ui-dev

# terminal 2 (for the Rust/App hot-reload)
npm run tauri dev
```